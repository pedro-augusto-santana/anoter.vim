** anoter.vim (v2) **
notetaking plugin for (n)vim.
should work with all OS's supporting vim without issues (maybe external link
handling), as it only uses pure vimscript.

** TODO: FINISH WRITING DOCS **
